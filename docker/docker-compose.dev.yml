version: '1.0'

services:
  odor-monitor-client_dev:
    ports:
      - 4000:4000
    build:
      context: ../
      dockerfile: Dockerfile.dev
    container_name: 'odor-monitor-client_dev'
    volumes:
      - ../src:/usr/src/app/src

